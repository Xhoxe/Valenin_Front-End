{
    "name": "nodejs-test",
    "private": true,
    "scripts": {
        "serve-cli": "live-server --port=3000 --host=localhost",
        "serve-node": "node server.js"
    },
    "scripts":{
      "format-code-html":"prettier --check --write index.html",
      "format-code-css":"prettier --check --write styles/**",
      "format-code-js":"prettier --check --write scripts/**"
    },
    "devDependencies": {
        "live-server": "^1.1.0",
        "prettier": "^2.7.1"
    }
}
